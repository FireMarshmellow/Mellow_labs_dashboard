# Copy to .env and set your registry image reference
# For GitHub Container Registry (GHCR): ghcr.io/<owner>/<repo>:latest
# Example: ghcr.io/mellow-labs/mellow_labs_dashboard:latest
IMAGE=ghcr.io/your-ghcr-user-or-org/mellow_labs_dashboard:latest

# Optional: set the host bind path for persistent data on your Synology NAS
# If not set, docker-compose will use the local ./data folder next to this file.
HOST_DATA_PATH=./data

# Optional: app version displayed in Settings page
# Typically set to the image tag (e.g., latest or v1.2.3)
APP_VERSION=latest
