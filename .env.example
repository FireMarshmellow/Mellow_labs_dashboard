# General Docker settings for production usage

# Full image reference to run in production. Keep lowercase user/org.
# Example GHCR image published by CI:
# ghcr.io/<owner>/mellow_biz:latest
IMAGE=ghcr.io/your-user-or-org/mellow_biz:latest

# Bind path for persistent data on the host. If not set, a named
# volume will be used automatically (app-data).
# Example Linux path: /srv/mellow-biz/data
# Example Windows path (Docker Desktop): C:/data/mellow-biz
HOST_DATA_PATH=

# Port to expose the app on the host
PORT=3000

# Optional: App version string shown in Settings (defaults to image tag)
APP_VERSION=latest
